import type {Metadata} from "next";import Link from "next/link";import {requireChatGPTUser} from "../../../chatgpt-auth";import {requireCatalogActor} from "../../../../db/catalog-repository";import {listReferralQueue} from "../../../../db/referral-repository";import {OperationsNav} from "../../operations-nav";
export const dynamic="force-dynamic";export const metadata:Metadata={title:"Referral queue",description:"Review and progress service referrals."};const status=(v:string)=>v.toLowerCase().replaceAll("_"," ");const date=(v:string)=>new Intl.DateTimeFormat("en-BD",{day:"numeric",month:"short",year:"numeric"}).format(new Date(v));
export default async function Page(){await requireChatGPTUser("/workspace/referrals/queue");const actor=await requireCatalogActor();const referrals=await listReferralQueue(actor);return <div className="member-page"><OperationsNav active="referrals" role={actor.role as "ADMIN"|"REFERRAL_OFFICER"|"ORG_REP"}/><main id="main-content" className="shell member-main"><div className="member-title"><p className="workspace-kicker">Service operations</p><h1>Referral queue</h1><p>Review new requests, ask for information and coordinate organization responses.</p></div><div className="referral-queue"><div className="referral-queue-row referral-queue-head"><span>Referral</span><span>Service</span><span>Submitted</span><span>Status</span><span>Action</span></div>{referrals.length?referrals.map(r=><div className="referral-queue-row" key={r.id}><span><b>{r.reference}</b><small>{r.assignedOfficerName??"Unassigned"}</small></span><span><b>{r.listingTitle}</b><small>{r.organization}</small></span><span>{date(r.createdAt)}</span><span><em className={`referral-status ${r.status.toLowerCase()}`}>{status(r.status)}</em></span><span><Link href={`/workspace/referrals/${r.reference}?queue=1`}>Review →</Link></span></div>):<div className="queue-feedback"><b>No active referrals</b><span>New requests will appear here.</span></div>}</div></main></div>}
