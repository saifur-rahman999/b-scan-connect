import type {Metadata} from "next";import Link from "next/link";import {requireChatGPTUser} from "../../../chatgpt-auth";import {requireCatalogActor} from "../../../../db/catalog-repository";import {getReferralTimeline} from "../../../../db/referral-repository";import {MemberNav} from "../../member-nav";import {ReferralActions} from "./referral-actions";
export const dynamic="force-dynamic";export const metadata:Metadata={title:"Referral details",description:"Track a B-SCAN Connect referral."};const status=(v:string)=>v.toLowerCase().replaceAll("_"," ");const time=(v:string)=>new Intl.DateTimeFormat("en-BD",{day:"numeric",month:"short",year:"numeric",hour:"numeric",minute:"2-digit"}).format(new Date(v));
async function Content({params,searchParams}:{params:Promise<{reference:string}>;searchParams:Promise<{queue?:string;created?:string}>}){const {reference}=await params;const query=await searchParams;const queueAccess=query.queue==="1";await requireChatGPTUser(`/workspace/referrals/${encodeURIComponent(reference)}${queueAccess?"?queue=1":""}`);const {referral,events,messages}=await getReferralTimeline(await requireCatalogActor(),reference,queueAccess);return <><MemberNav active="referrals"/><main id="main-content" className="shell member-main">{query.created==="1"&&<div className="success-notice" role="status"><span>✓</span><div><b>Referral submitted</b><p>Your reference is {referral.reference}. You can return here to follow every update.</p></div></div>}<nav className="breadcrumbs" aria-label="Breadcrumb"><Link href={queueAccess?"/workspace/referrals/queue":"/workspace/referrals"}>{queueAccess?"Referral queue":"My referrals"}</Link><span>/</span><span>{referral.reference}</span></nav><div className="referral-detail-title"><div><span className={`referral-status ${referral.status.toLowerCase()}`}>{status(referral.status)}</span><h1>{referral.listingTitle}</h1><p>{referral.organization} · {referral.district}</p></div><div><small>Referral reference</small><strong>{referral.reference}</strong></div></div><div className="referral-detail-grid"><div className="referral-detail-main"><section><h2>Your request</h2><p>{referral.requestSummary}</p><dl><div><dt>Submitted</dt><dd>{time(referral.createdAt)}</dd></div><div><dt>Assigned officer</dt><dd>{referral.assignedOfficerName??"Waiting for assignment"}</dd></div></dl></section><section><h2>Referral timeline</h2><div className="referral-timeline">{events.map(e=><article key={e.id}><span aria-hidden="true">✓</span><div><b>{e.summary}</b><p>{e.actorName} · {time(e.createdAt)}</p></div></article>)}</div></section><section><h2>Messages</h2>{messages.length?<div className="referral-messages">{messages.map(m=><article key={m.id}><div><b>{m.authorName}</b><small>{time(m.createdAt)}</small></div><p>{m.message}</p></article>)}</div>:<p className="muted-copy">No messages yet. Updates and questions will appear here.</p>}</section></div><ReferralActions reference={referral.reference} status={referral.status} queueAccess={queueAccess}/></div></main></>}
export default function Page(props:{params:Promise<{reference:string}>;searchParams:Promise<{queue?:string;created?:string}>}){return <div className="member-page"><Content {...props}/></div>}
